using UnityEngine;
using System.Collections;

public class WinCondition : MonoBehaviour
{
    [SerializeField]
    Health[] _healthBars;

    void Update()
    {
        foreach (Health healthBar in _healthBars)
        {

        }
    }
}
